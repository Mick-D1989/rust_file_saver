[project]
name = "file-saver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Michael Durante", email = "michaeldurante@hotmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "debugpy>=1.8.12",
    "flask>=3.1.0",
    "flask-cors>=5.0.0",
    "gunicorn>=23.0.0",
    "maturin>=1.8.2",
    "werkzeug>=3.1.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
